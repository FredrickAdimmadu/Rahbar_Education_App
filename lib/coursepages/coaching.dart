import 'package:flutter/material.dart';

class CoachingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coaching Course'),
      ),
      body: Center(
        child: Text('Welcome '),
      ),
    );
  }
}
