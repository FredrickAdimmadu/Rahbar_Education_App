import 'package:flutter/material.dart';

class DataanalysisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Data Analysis Course'),
      ),
      body: Center(
        child: Text('Welcome '),
      ),
    );
  }
}
