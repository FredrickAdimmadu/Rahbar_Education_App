import 'package:flutter/material.dart';

class MarkettingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Marketting Course'),
      ),
      body: Center(
        child: Text('Welcome '),
      ),
    );
  }
}
