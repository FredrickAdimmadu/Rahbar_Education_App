import 'package:flutter/material.dart';

class PoliticsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Politics Course'),
      ),
      body: Center(
        child: Text('Welcome '),
      ),
    );
  }
}
